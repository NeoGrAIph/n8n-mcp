{
  "name": "synestra-n8n-gitops-mcp",
  "version": "0.1.0",
  "description": "Synestra n8n GitOps MCP extension for workflow Code/Set raw files",
  "license": "UNLICENSED",
  "type": "module",
  "main": "src/index.mjs",
  "scripts": {
    "check": "find src tests -name '*.mjs' -print0 | xargs -0 -n1 node --check",
    "smoke:docker": "docker build -t synestra-n8n-gitops-mcp:local . && sh tests/smoke.sh synestra-n8n-gitops-mcp:local",
    "start": "node src/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm test && npm run check"
  },
  "engines": {
    "node": ">=22"
  }
}
